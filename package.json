{
  "name": "retail-pricing-app",
  "version": "1.0.0",
  "private": true,
  "description": "Retail pricing feed management SPA — enterprise grade",
  "scripts": {
    "dev": "concurrently -n \"API,UI\" -c \"blue,green\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "install:all": "npm install && npm install --workspace=backend && npm install --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "seed": "npm run db:seed --workspace=backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
